import server from './server'
import env from './env'
import api from './api'
import name from './name'

let config = {
  server: server,
  api: api,
  env: env,
  name: name
};

export default config;
